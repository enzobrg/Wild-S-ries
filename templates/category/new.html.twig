{% extends "base.html.twig" %}

{% block body %}
<div class="text-light text-center mt-4">
    <h1>Want to add new category? Go here ⬇️</h1>
</div>
<div class="text-light mt-5">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Add new category ▶️</button>
    {{ form_end(form) }}
</div>
{% endblock %}
