{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="media m-auto w-50">
        <div class="media-body">
            <h1 class="mb-5 text-center mt-4">{{ season.description }} de {{ program.title }}</h1>
            <p>{{ program.summary }}</p>
        </div>
        <div class="accordion" id="accordionExample">
            <h2 class="mb-5">Liste des episodes</h2>
            {% for episode in episodes %}
                <h4><a href="{{ path('program_episode_show', { program : program.id, season : season.id, episode : episode.id }) }}">Episode {{ episode.number }} : {{ episode.title }}</a></h4>
            {% endfor %}
        </div>
        <a class="btn btn-warning" href="{{ path('app_index') }}">Retour à l'accueil</a>
    </div>
{% endblock %}
