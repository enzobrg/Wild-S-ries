{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center text-light">Edit Episode</h1>

    <div class="text-light">
    {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <a href="{{ path('episode_index') }}" class="text-center text-light">back to list</a>

    {{ include('episode/_delete_form.html.twig') }}
{% endblock %}
