{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-light text-center mt-4">Edit Season</h1>

    <div class="text-light mt-4">
    {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <a href="{{ path('season_index') }}" class="text-light nav-link">Back to list</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
